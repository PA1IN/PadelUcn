version: '3.8'
services:
  padelucn-db:
    image: postgres:latest
    container_name: padelucn-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ingeso
      POSTGRES_PASSWORD: 12342
      POSTGRES_DB: padelucn    
    volumes:
      - padelucn_data:/var/lib/postgresql/data
      - ./backend/ingeso-back/database-schema-new.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./backend/ingeso-back/sample-data-new.sql:/docker-entrypoint-initdb.d/02-sample-data.sql
      - ./backend/ingeso-back/add-missing-columns.sql:/docker-entrypoint-initdb.d/03-add-missing-columns.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ingeso -d padelucn"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - padelucn-network

  padelucn-backend:
    platform: linux/amd64  # Especificamos la plataforma explícitamente
    build:
      context: ./backend/ingeso-back
      dockerfile: Dockerfile
    container_name: padelucn-backend
    ports:
      - "8080:8080"
    depends_on:
      padelucn-db:
        condition: service_healthy
    environment:
      - NODE_ENV=development
      - DB_HOST=padelucn-db
      - DB_PORT=5432
      - DB_USER=ingeso
      - DB_PASSWORD=12342
      - DB_NAME=padelucn
      - PORT=8080
    restart: unless-stopped
    networks:
      - padelucn-network

volumes:
  padelucn_data:

networks:
  padelucn-network:
    driver: bridge